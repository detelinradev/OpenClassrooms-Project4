package com.parkit.parkingsystem.constants;

/**
 * Holds vehicle's charging rates per hour as <code>double</code> variable
 * for use in the application
 */
public class Fare {

    /**
     * Bike charging rate per hour as <code>double</code> variable
     */
    public static final double BIKE_RATE_PER_HOUR = 1.0;

    /**
     * Car charging rate per hour as <code>double</code> variable
     */
    public static final double CAR_RATE_PER_HOUR = 1.5;
}
